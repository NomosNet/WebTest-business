@{
    ViewData["Title"] = "Главная";
}

<div class="text-center">
    <h1 class="display-4">Тест функционала заказчика и менеджера</h1>
    @if (!(User?.Identity?.IsAuthenticated ?? false))
    {
        <p>Используйте верхнее меню или ссылки ниже.</p>

        <p class="mt-3">
            <a class="btn btn-primary" asp-controller="Account" asp-action="Login">Войти</a>
        </p>

        <p class="mt-3">
            Тестовые пользователи:
        </p>
        <ul class="list-unstyled">
            <li>Менеджер: <code>manager / manager</code></li>
            <li>Заказчик: <code>customer / customer</code></li>
        </ul>

        <p>
            После входа заказчиком: разделы <strong>Каталог</strong>, <strong>Мои заказы</strong>.<br />
            После входа менеджером: раздел <strong>Панель менеджера</strong>.
        </p>
    }
    else
    {
        <p>Добро пожаловать, <strong>@User.Identity!.Name</strong>!</p>
        @if (User.IsInRole("Customer"))
        {
            <p>
                Используйте разделы <strong>Каталог</strong> и <strong>Мои заказы</strong> в верхнем меню.
            </p>
        }
        else if (User.IsInRole("Manager"))
        {
            <p>
                Используйте раздел <strong>Панель менеджера</strong> в верхнем меню.
            </p>
        }
    }
</div>
